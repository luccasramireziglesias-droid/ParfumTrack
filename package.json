{
  "name": "parfumtrack",
  "version": "1.0.0",
  "private": true,
  "description": "PWA de gestión de ventas para revendedores de perfumes",
  "main": "worker.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:build": "npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "keywords": [],
  "author": "Parfum Track <parfumtrack@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "serve": "^14.2.6",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1"
  }
}
